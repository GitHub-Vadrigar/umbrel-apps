services:
  nerva:
    container_name: nerva
    build:
      context: ./vadrigar-nerva
      dockerfile: ./Dockerfile

    restart: unless-stopped

    ports:
      - "17566:17566"   # RPC ONLY

    volumes:
      - ${APP_DATA_DIR}/data/nerva:/root/.nerva
