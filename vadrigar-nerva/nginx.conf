server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /json_rpc {
        proxy_pass http://nervad:17566/json_rpc;
    }
}